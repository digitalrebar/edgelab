Name: "k3s-cluster"
Description: "Build K3s cluster"
Documentation: |
  Builds a k3s cluster
  Uses the task-library cluster building components
Stages:
  - "cluster-setup"
  - "k3s-install"
Meta:
  color: "yellow"
  icon: "ship"
  title: "k3s cluster"
  k3s: "true"
  copyright: "RackN 2019"
